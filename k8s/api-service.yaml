apiVersion: v1
kind: Service
metadata:
  name: paypilot-api
  namespace: paypilot
  labels:
    app.kubernetes.io/name: paypilot-api
    app.kubernetes.io/part-of: paypilot
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: paypilot-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
